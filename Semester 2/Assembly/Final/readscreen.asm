.MODEL SMALL
.STACK 100H
.DATA
ARRAY DB 2000 DUP(?)
.CODE
MOV AX, @DATA
MOV DS,AX

MOV AX,0XB800

MOV ES,AX

MOV BYTE PTR ES:[SI],'1'
MOV BYTE PTR ES:[SI+1],0x7

MOV SI,0X0000 
MOV BX,OFFSET ARRAY

MOV CX,2000

ADDLOOP:

MOV AL, BYTE PTR ES:[SI]

MOV BYTE PTR [BX],AL

INC BX

ADD SI,2

LOOP ADDLOOP

HLT


.EXIT